branches:
  only:
    - master
language: php
php:
  - "5.3"
before_script:
  - ping -c 3 projects.bit3.de
  - wget http://packages.bit3.de/packages.json -O -
  - composer require --dev bit3/php-coding-standard:@dev
  - composer require --dev squizlabs/php_codesniffer:1.*
script: ant
